import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

alert("Your future will shine brightly if you approach this project with 100% consistency, authenticity, and a commitment to self-improvement.");

const rootElement = document.getElementById('root');
const root = ReactDOM.createRoot(rootElement);

root.render(
    <App/>
)